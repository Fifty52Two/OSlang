// Static variable
static int counter <- 0;

semaphore mutex <- 1;

process Producer(burst: 3, priority: 2, arrival: 0) {
    wait(mutex);
    counter <- counter + 1;
    post(mutex);
}

run(Sys1, until: 20);